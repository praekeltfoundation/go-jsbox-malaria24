/*jshint -W083 */

// Shared utils lib
// Nothing here yet.
go.utils = {

    // Handy to leave at the bottom to ensure trailing commas in objects
    // don't become syntax errors.
    "commas": "commas"
};
